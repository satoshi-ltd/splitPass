import StyleSheet from 'react-native-extended-stylesheet';

export const style = StyleSheet.create({
  screen: {
    height: '100%',
    paddingTop: '$viewOffset * 3',
  },
  container: {
    backgroundColor: '$colorBase',
  },
});
