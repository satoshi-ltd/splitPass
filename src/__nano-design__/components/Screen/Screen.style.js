import StyleSheet from 'react-native-extended-stylesheet';

export const style = StyleSheet.create({
  safeAreaView: {
    flex: 1,
  },

  scrollView: {
    // flex: 1,
  },

  view: {
    // gap: '$spaceL',
    // padding: '$spaceM',
  },
});
