import StyleSheet from 'react-native-extended-stylesheet';

export const style = StyleSheet.create({
  notification: {
    gap: '$viewOffset / 2',
    paddingBottom: '$viewOffset / 2',
    paddingTop: '$viewOffset / 2',
  },
});
